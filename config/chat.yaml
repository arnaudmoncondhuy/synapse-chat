# Configuration for synapse-chat package
# API endpoints and chat widget configuration

services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: false

  # ── API Controllers ────────────────────────────────────────────────────────
  ArnaudMoncondhuy\SynapseChat\Controller\Api\:
    resource: '../src/Controller/Api/'
    tags: ['controller.service_arguments']

  ArnaudMoncondhuy\SynapseChat\Controller\UI\:
    resource: '../src/Controller/UI/'
    tags: ['controller.service_arguments']
